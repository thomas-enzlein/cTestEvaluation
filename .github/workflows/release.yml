# Baut das Windows-Setup automatisch und haengt es an das Release.
#
# Zwei Wege:
#   - Tag v* pushen               -> baut, legt das Release an und haengt das Setup an
#   - manuell starten (Actions -> Windows-Setup -> Run workflow)
#                                 -> baut und legt das Setup als Artefakt ab
#                                    (zum Testen des Ablaufs, ohne Release)
#
# Der eigentliche Bau steckt in build/build_factory.ps1 und laeuft lokal genau
# gleich; dieser Workflow installiert nur die Voraussetzungen und ruft es auf.

name: Windows-Setup

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version (leer = APP_VERSION aus app.R)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  setup:
    name: Setup bauen
    runs-on: windows-2022
    timeout-minutes: 120
    env:
      R_VERSION: "4.5.3"
      SNAPSHOT: "2026-08-01"

    steps:
      # Volle Historie und alle Tags holen: der Release-Text braucht die Commits
      # seit dem letzten Tag (git log <letzter Tag>..<Tag>)
      - name: Quellcode holen
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Auslieferungsordner festlegen. Zwei Bedingungen:
      #   - ausserhalb des Repositories, sonst spiegelt robocopy /MIR das
      #     Repository in sich selbst
      #   - ohne ".." im Pfad, denn upload-artifact und cache verbieten
      #     relative Pfadangaben
      - name: Auslieferungsordner festlegen
        id: factory
        shell: pwsh
        run: |
          $pfad = Join-Path (Split-Path $env:GITHUB_WORKSPACE -Parent) "_ctest_build"
          "FACTORY=$pfad" >> $env:GITHUB_ENV
          "factory=$pfad" >> $env:GITHUB_OUTPUT
          Write-Host "Auslieferungsordner: $pfad"

      # R und pandoc werden hier nur fuer die Testsuite gebraucht. Das Setup
      # bringt sein eigenes R und pandoc mit (build_factory.ps1).
      - name: R bereitstellen
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ env.R_VERSION }}
          use-public-rspm: true

      - name: pandoc bereitstellen
        uses: r-lib/actions/setup-pandoc@v2

      - name: Pakete fuer die Testsuite installieren
        shell: pwsh
        run: |
          # Die Tests laden die App (global.R) und brauchen daher ALLE Pakete aus
          # req.txt; req_dev.txt liefert testthat und withr dazu. Derselbe
          # Snapshot wie im Setup, damit gleich gebaut und getestet wird.
          Rscript -e "pkgs <- c(readLines('req.txt'), readLines('req_dev.txt')); pkgs <- trimws(pkgs[nzchar(trimws(pkgs))]); fehlend <- pkgs[!(pkgs %in% rownames(installed.packages()))]; cat('zu installieren:', if (length(fehlend)) paste(fehlend, collapse = ', ') else 'nichts', '\n'); if (length(fehlend)) install.packages(fehlend, repos = 'https://packagemanager.posit.co/cran/${{ env.SNAPSHOT }}')"

      - name: Testsuite
        shell: pwsh
        run: Rscript tests/run_tests.R

      - name: Inno Setup bereitstellen
        shell: pwsh
        run: |
          $kandidaten = @(
            "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe",
            "${env:ProgramFiles}\Inno Setup 6\ISCC.exe"
          )
          if ($kandidaten | Where-Object { Test-Path $_ }) {
            Write-Host "Inno Setup ist vorhanden"
          } else {
            Write-Host "Inno Setup fehlt - wird installiert"
            choco install innosetup -y --no-progress
          }

      - name: Chrome bereitstellen
        shell: pwsh
        run: |
          $kandidaten = @(
            "${env:ProgramFiles}\Google\Chrome\Application\chrome.exe",
            "${env:ProgramFiles(x86)}\Google\Chrome\Application\chrome.exe"
          )
          if ($kandidaten | Where-Object { Test-Path $_ }) {
            Write-Host "Chrome ist vorhanden"
          } else {
            Write-Host "Chrome fehlt - wird installiert"
            choco install googlechrome -y --no-progress
          }

      # Nur die R-Paketbibliothek wird gecacht: der teuerste Teil des Baus.
      # Schluessel enthaelt R-Version, CRAN-Snapshot und req.txt - aendert sich
      # einer davon, wird neu installiert.
      - name: R-Paketbibliothek aus dem Cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.factory.outputs.factory }}\R\library
          key: rlib-${{ runner.os }}-r${{ env.R_VERSION }}-snap${{ env.SNAPSHOT }}-${{ hashFiles('req.txt') }}
          restore-keys: |
            rlib-${{ runner.os }}-r${{ env.R_VERSION }}-snap${{ env.SNAPSHOT }}-

      - name: Version bestimmen
        id: version
        shell: pwsh
        run: |
          $appVersion = (Select-String -Path app.R -Pattern 'APP_VERSION\s*<-\s*"([^"]+)"').Matches[0].Groups[1].Value
          if ("${{ github.ref_type }}" -eq "tag") {
            $tagVersion = "${{ github.ref_name }}".TrimStart("v")
            if ($tagVersion -ne $appVersion) {
              throw "Tag ${{ github.ref_name }} passt nicht zu APP_VERSION ($appVersion) in app.R"
            }
            $version = $appVersion
          } elseif ("${{ github.event.inputs.version }}") {
            $version = "${{ github.event.inputs.version }}"
          } else {
            $version = $appVersion
          }
          "version=$version" >> $env:GITHUB_OUTPUT
          Write-Host "Version: $version (APP_VERSION in app.R: $appVersion)"

      - name: Setup bauen
        shell: pwsh
        run: |
          .\build\build_factory.ps1 -FactoryDir "$env:FACTORY" -Version "${{ steps.version.outputs.version }}" -RVersion "$env:R_VERSION" -Snapshot "$env:SNAPSHOT"

      - name: Protokoll des Baus anzeigen
        shell: pwsh
        run: |
          Get-Content "$env:FACTORY\build-info.txt"
          Get-ChildItem "$env:FACTORY\Output" | Format-Table Name, Length, LastWriteTime

      - name: Setup als Artefakt ablegen
        if: ${{ github.ref_type != 'tag' }}
        uses: actions/upload-artifact@v4
        with:
          name: ctest-setup-${{ steps.version.outputs.version }}
          path: ${{ steps.factory.outputs.factory }}\Output\*.exe
          retention-days: 7

      # Release-Text bauen: der Abschnitt der Version aus CHANGELOG.md, darunter
      # die Commits seit dem letzten Tag und die Vergleichs-URL. Die
      # GitHub-Automatik (--generate-notes) faellt weg, weil sie nur gemergte
      # Pull Requests auflistet - hier wird direkt auf main committet.
      - name: Release-Text vorbereiten
        if: ${{ github.ref_type == 'tag' }}
        id: notes
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $tag = "${{ github.ref_name }}"
          $repo = "${{ github.repository }}"
          $teile = @()

          if (Test-Path "CHANGELOG.md") {
            # Encoding ausdruecklich: damit Umlaute unabhaengig von den
            # Voreinstellungen der Shell stimmen
            $md = Get-Content "CHANGELOG.md" -Raw -Encoding UTF8
            $muster = "(?ms)^##[^\r\n]*\b$([regex]::Escape($version))\b[^\r\n]*\r?\n(.*?)(?=^##\s|\z)"
            $treffer = [regex]::Match($md, $muster)
            if ($treffer.Success) {
              $teile += $treffer.Groups[1].Value.Trim()
            } else {
              Write-Warning "CHANGELOG.md hat keinen Abschnitt fuer Version $version - es wird nur die Aenderungsliste verwendet."
            }
          } else {
            Write-Warning "CHANGELOG.md fehlt - es wird nur die Aenderungsliste verwendet."
          }

          $letzterTag = (git tag --sort=-creatordate | Where-Object { $_ -ne $tag } | Select-Object -First 1)
          if ($letzterTag) {
            $alle = @(git log "$letzterTag..$tag" --no-merges --pretty="- %s (%h)")
            $vergleich = "**Vergleich:** https://github.com/$repo/compare/$letzterTag...$tag"
          } else {
            $alle = @(git log $tag --no-merges --pretty="- %s (%h)")
            $vergleich = "**Vergleich:** https://github.com/$repo/commits/$tag"
          }
          # Bei sehr vielen Commits nur die neuesten nennen
          $grenze = 30
          if ($alle.Count -gt $grenze) {
            $commits = @($alle[0..($grenze - 1)]) + @("- ... und $($alle.Count - $grenze) weitere Commits (siehe Vergleich unten)")
          } else {
            $commits = $alle
          }
          if ($commits.Count -gt 0) {
            $teile += "<details><summary>Aenderungen im Detail (Commits)</summary>`n`n" +
                      (($commits -join "`n")) + "`n`n</details>"
          }
          $teile += $vergleich

          # ohne BOM schreiben: PowerShell 5.1 haengt bei -Encoding UTF8 eines an
          $datei = Join-Path $env:RUNNER_TEMP "release-notes.md"
          [System.IO.File]::WriteAllText($datei, ($teile -join "`n`n"),
                                        (New-Object System.Text.UTF8Encoding($false)))
          "datei=$datei" >> $env:GITHUB_OUTPUT
          Write-Host "--- Release-Text ---"
          Get-Content $datei -Encoding UTF8

      - name: Release anlegen und Setup anhaengen
        if: ${{ github.ref_type == 'tag' }}
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $setup = (Get-ChildItem "$env:FACTORY\Output\*.exe" | Select-Object -First 1).FullName
          $tag = "${{ github.ref_name }}"
          $notizen = "${{ steps.notes.outputs.datei }}"
          Write-Host "Setup:   $setup"
          Write-Host "Notizen: $notizen"
          # cmd /c, damit die Fehlermeldung von gh nicht als PowerShell-Fehler gilt
          cmd /c "gh release view $tag >nul 2>&1"
          if ($LASTEXITCODE -eq 0) {
            Write-Host "Release $tag existiert - Setup und Text werden aktualisiert"
            gh release upload $tag $setup --clobber
            gh release edit $tag --notes-file $notizen
          } else {
            gh release create $tag $setup --title "Version ${{ steps.version.outputs.version }}" --notes-file $notizen
          }
          if ($LASTEXITCODE -ne 0) { throw "Release $tag konnte nicht erstellt werden." }
