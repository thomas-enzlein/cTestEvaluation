## Kohorte `r d$buchstabe` (`r d$klassen_kombi`)

```{r, echo=FALSE}
if (!is.null(d$tabelle)) tabelle_a(d$tabelle, d$tabelle_differenz)
```

Veränderungen sind farbig hervorgehoben.

```{r, echo=FALSE}
if (!is.null(d$rangliste)) {
  .tabellen_abstand(tabelle_b(d$rangliste, d$rangliste_unten),
                    titel = d$rangliste_titel)
}
```
